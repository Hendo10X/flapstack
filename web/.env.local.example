NEXT_PUBLIC_API_URL=http://localhost:8080

# Groq API key — get one free at https://console.groq.com
GROQ_API_KEY=

# Polar checkout URL — the page the paywall button opens. After successful
# checkout, redirect customers to /checkout/success?token=<the-token-from-webhook>.
NEXT_PUBLIC_POLAR_CHECKOUT_URL=
